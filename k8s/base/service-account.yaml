# Service Account for Pub/Sub access
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pubsub-service-account
  namespace: pharmacy-system
  annotations:
    # Replace with your GCP service account email
    iam.gke.io/gcp-service-account: pharmacy-pubsub@YOUR_PROJECT_ID.iam.gserviceaccount.com
