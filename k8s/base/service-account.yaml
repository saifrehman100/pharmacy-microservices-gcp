# Service Account for Pub/Sub access
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pubsub-service-account
  namespace: pharmacy-system
  annotations:
    # GCP service account email for Workload Identity
    iam.gke.io/gcp-service-account: pharmacy-pubsub@pharmacy-ms-458074.iam.gserviceaccount.com
